# 电商 AI 项目执行计划（优化版 v2.0）

> 项目目录：`D:\ai\dianshang`
>
> 项目定位：面向多平台中大型电商商家的智能运营工作台 + 全平台 AI 客服统一中台。
>
> 核心差异化：不与单平台免费智能客服竞争，不与专业 ERP/WMS 竞争，重点建设跨平台统一管理、智能运营驾驶舱和每日补货决策。

## 0. 当前恢复点（2026-09-04）

- 当前分支：`feat/warehouse-mvp`。
- 工作区包含多个阶段的未提交实现，**不要执行 `git reset --hard`、`git clean -fd`、切换分支或强制覆盖**。
- 当前全量测试基线：`115 passed`；以实际命令结果为准，不以 pytest cache 为准。
- 已完成商品工作台、异步任务、PDF/DOCX 文档、RAG、只读 Agent、Alembic、服务稳定性、上传安全硬化、员工登录与角色、仓储 MVP、离线外部连接器和运营看板。
- workspace 全链路隔离第一轮已完成：业务数据归属、legacy 回填、后台任务与文档索引上下文、RAG workspace 过滤、外部接口上下文、库存/入库范围校验和空间内业务键已接通。
- 库存策略与低库存告警第一轮已完成；外部同步运行记录、快照新鲜度、外部同步失败/快照过期告警和 Dashboard 摘要已有首版。
- 下一阶段从**统一外部订单/销量事实层**开始，再实现智能运营驾驶舱 MVP。

## 1. 产品定位与业务边界

### 1.1 产品定位

> **面向多平台中大型电商商家的智能运营工作台：统一沉淀平台商品、订单、库存和客服知识，帮助运营人员每天快速完成库存健康检查与补货决策。**

核心功能线：

1. **智能运营驾驶舱**：每日一屏查看所有 SKU 的库存健康度、销量趋势和补货建议。
2. **活动销量预测**：基于历史活动和日常销量，估算大促销量与补货缺口。
3. **补货决策闭环**：运营专员确认/修改建议，运营主管审批，形成采购申请并跟踪入库。
4. **全平台统一客服中台**：一套 workspace 知识库 + RAG，覆盖多个销售渠道。
5. **轻量仓储协同**：入库协同、库存台账和跨平台库存信息感知。
6. **可选产线排期建议**：为有自产能力的客户提供理论排产建议，不替代正式生产计划。

不做：

- 不做单平台极致客服，不与店小蜜、京小智等平台原生产品正面竞争。
- 不做复杂 ERP/WMS，不实现库位级精细化、波次、拣货优化。
- 不做自动交易：不自动下单、付款、退款、改价、取消订单，也不默认向平台回写交易动作。

### 1.2 核心用户

- **运营专员**：每天查看 SKU 健康度、确认/修改补货建议、生成采购申请；目标是减少手工汇总和漏检。
- **运营主管**：查看全局健康度、审批补货申请、管理运营人员；目标是及时发现异常并控制供应链风险。
- **老板/店主**：查看商家整体经营与供应链健康度，做战略决策。
- **库管**：执行入库、出库和盘点等仓库操作，不负责补货审批。
- **客服**：读取统一商品、库存、订单和知识库信息，生成辅助回复。

### 1.3 角色和数据边界

- **Workspace**：代表一个商家/品牌，是最大业务隔离边界。
- **用户**：属于 workspace；同一商家内按角色和数据范围协作，不按用户默认完全隔离。
- **角色**：管理员/店主、运营主管、运营专员、库管、客服、只读成员。
- **店铺/仓库/SKU 范围**：控制用户可查看和操作的数据范围。
- **审批状态**：建议确认、采购申请审批、库存入账是不同动作，不能用一个按钮混合。

建议补货流程：

```text
系统生成建议
→ 运营专员确认/修改/忽略
→ 生成采购申请
→ 运营主管审批
→ 外部采购或理论排产
→ 仓库反馈入库
→ 运营确认入账
```

### 1.4 外部平台边界

平台连接器按阶段推进：

| 阶段 | 平台 | 说明 |
|---|---|---|
| 第一阶段 | 无真实平台 | 手工/CSV/JSON/mock，先跑通统一事实层 |
| 第二阶段 | 淘宝、抖音 | 有明确授权后优先做只读适配 |
| 第三阶段 | 京东、拼多多 | 复用统一 Adapter/Connector 扩展 |
| 第四阶段 | Amazon 等跨境 | 扩展 ASIN、seller SKU、marketplace、FBA 字段 |

统一原则：

- Webhook + 定时轮询补偿，统一称准实时，不承诺严格实时。
- 所有平台数据先进入统一事件 Inbox，执行幂等、版本和对账处理。
- 平台连接器默认只读，不做自动下单、付款、退款、改价或取消订单。
- 外部库存是事实观察值；本系统内部库存是经过确认的库存投影，两者不能混称。
- 平台认证、签名、限流、回调和资质要求以实施时对应官方文档为准。

## 2. 已完成

### 第一周：商品工作台

- [x] 本地 fixture 商品采集、字段校验、标准化和幂等 Upsert
- [x] 商品查询、详情、价格历史 API 和极简网页展示
- [x] SQLite / MySQL 配置切换

### 第二周：客服知识库与工程化

- [x] ThreadPoolExecutor 异步任务、SQL claim、lease、heartbeat、取消和指数退避重试
- [x] PDF / DOCX 上传、SHA-256 文档版本化、后台解析、chunk 和商品关联
- [x] 本地确定性 Embedder、JSON 向量库、top-k/阈值检索、引用和无答案兜底
- [x] CLI（`init-db`、`seed-docs`、`enqueue-crawl`、`query`、`status`）和 `/api/settings`
- [x] 只读 Agent 工具白名单、Schema 校验、限流和审计日志
- [x] Alembic baseline、增量修复迁移、幂等升级和 legacy 库兼容初始化
- [x] 后台 worker 生命周期、lease 恢复、readiness、请求 ID和通用错误兜底
- [x] 上传签名/路径/SHA 校验、Agent 限流、审计摘要脱敏和 CSRF
- [x] 员工 session 登录、workspace 成员、角色和仓库范围授权
- [x] 仓储 MVP：SKU、仓库、预计入库、实收反馈、差异确认、库存流水与余额
- [x] 离线 JSON/CSV/mock 外部连接器、外部快照、事件 Inbox 和只读对账基础
- [x] 运营概览看板第一版
- [x] workspace 全链路隔离第一轮，迁移 `0007_workspace_iso`
- [x] 库存策略与低库存告警第一轮，迁移 `0008_inventory_alerts`
- [x] 外部同步运行记录、快照新鲜度和外部健康告警首版，迁移 `0009_sync_health`

当前全量回归：

```text
115 passed
```

## 3. 第一阶段：统一外部事实层（当前下一步）

驾驶舱依赖订单和销量数据，因此先建立统一事实层，不立即接真实平台。

### 3.1 目标

```text
JSON / CSV / mock 平台数据
→ 统一平台/店铺标识
→ 外部订单与订单明细
→ 外部 SKU 映射
→ 每日 SKU 销量聚合
→ 驾驶舱读取
```

### 3.2 建议模型

```text
external_accounts
external_product_mappings
external_orders
external_order_lines
daily_sku_sales
```

最低字段应保留：

- workspace、platform、account/store
- 外部订单号、订单状态、支付/创建/更新时间
- 外部 SKU、内部 SKU 映射、数量、成交/退款金额
- payload hash、幂等键、来源模式、模拟标记、事件版本
- 同步时间和数据完整度

订单只作为外部事实进入系统，不自动执行平台交易动作。

### 3.3 销量口径

第一版同时提供：

- 近 7 日销量
- 近 14 日销量
- 近 30 日销量
- 有效销售天数
- 缺货天数/数据完整度（如数据可得）
- 取消、退款和异常订单的处理口径

日均销量不简单用自然日总数相除：

```text
日均销量 = 有效销量 / 有效销售天数
```

新 SKU 或数据不足时显示“数据不足”，不生成伪精确预测。缺货期间销量不能直接当作正常零销量，需在口径中标记。

### 3.4 第一阶段验收标准

- 两个 workspace 可导入相同外部订单号/SKU，不互相覆盖。
- 同一订单重复导入为 no-op，不重复计销量。
- 取消/退款口径可解释并有测试。
- 能按 workspace、平台、店铺、SKU 查询日销量。
- 外部订单导入不修改 `InventoryBalance` 或 `InventoryTransaction`。
- 全量测试和迁移测试通过。

## 4. 第二阶段：智能运营驾驶舱 MVP（核心差异化）

驾驶舱是登录后的默认运营首页，第一版先做确定性、可解释的补货建议，再逐步增加预测模型。

### 4.1 首页第一屏

展示：

- 今日待处理补货 SKU 数
- 紧急补货数
- 建议补货数
- 数据不足 SKU 数
- 同步异常数
- 待运营主管审批数
- 库存健康分布

### 4.2 SKU 健康表

每行一个 SKU，至少包含：

| 字段 | 口径 |
|---|---|
| 商品/SKU | `products`、`product_skus` |
| 当前内部库存 | 已确认内部良品 `on_hand`，后续扣除已确认预占 |
| 近 7/14/30 日销量 | 统一事实层聚合 |
| 日均销量 | 有效销量 / 有效销售天数 |
| 可售天数 | 当前决策库存 / 日均销量；日均为 0 时显示特殊状态 |
| 安全库存/补货点 | workspace 内仓库-SKU 策略 |
| 建议补货量 | 确定性公式和覆盖天数 |
| 建议原因 | 低库存、趋势、活动或数据不足 |
| 数据完整度 | 同步窗口、缺货和异常数据说明 |
| 状态/操作 | 确认、修改、忽略、查看依据 |

建议公式：

```text
目标库存 = 日均销量 × 覆盖天数 + 安全库存
建议补货量 = max(0, 目标库存 - 当前决策库存)
```

颜色只作辅助，必须同时显示文字：

- 红：可售天数 < 3 天，紧急补货
- 黄：3–7 天，建议补货
- 绿：7–30 天，正常
- 灰：> 30 天，库存偏高/可能积压
- 灰色特殊状态：数据不足、无销量、同步异常

### 4.3 补货决策状态

建议新增 `restock_suggestions`，保留：

```text
suggested
confirmed
modified
ignored
submitted
approved
cancelled
fulfilled
```

第一版可以只实现：

```text
suggested → confirmed/modified/ignored → submitted
```

运营专员只能处理授权范围内 SKU；运营主管可查看全局并审批；生成采购申请不等于库存已入账。

### 4.4 第二阶段暂缓

- 不一开始承诺真实 AI 预测准确率。
- 不一开始实现复杂采购系统。
- 不一开始实现正式产线排程。
- 不一开始计算未经验证的 GMV、周转率和动态补货。
- 不接真实平台生产 API，先用 mock/CSV 证明每日工作流。

## 5. 第三阶段：活动销量预测

### 5.1 活动模型

```text
activities
activity_history
```

至少保存：

- workspace、平台、店铺、活动类型、名称、开始/结束时间
- SKU、普通期间销量、活动期间销量、活动天数
- 计算倍率、数据质量、人工修正和来源

活动倍率建议：

```text
活动期间日均销量 / 同长度普通期间日均销量
```

首版可支持“人工输入活动倍率 + 系统计算缺口”，历史数据足够后再自动估算。

### 5.2 展示

```text
平时日均 × 活动倍率 = 活动预测日均
活动预测日均 × 活动天数 = 活动总需求
活动总需求 - 当前决策库存 = 活动补货缺口
```

活动期间缺货、新 SKU、历史不足、活动重叠时明确显示数据质量状态。

## 6. 第四阶段：库存策略、告警与对账补强

当前已完成首版：

- 安全库存策略
- 低库存告警
- 告警去重、确认、恢复和重开基础逻辑
- 外部同步运行记录
- 快照 freshness
- 外部同步失败/快照过期告警
- Dashboard 告警摘要

后续补强：

- 对账结果持久化与批量对账
- 外部库存与内部库存差异告警
- 告警自动调度、失败重试和补偿
- 告警聚合、降噪和 SLA
- 飞书/钉钉等出站通知（验签、幂等和人工审批回调）
- 外部账户/映射正式管理

外部数量只做观察和对账，不写入内部库存流水。

## 7. 第五阶段：RAG 固定评测与模型抽象

### 7.1 评测维度

- 检索命中率
- 引用覆盖率
- 无答案率
- 拒答准确性
- 响应耗时 P50/P95/P99
- 跨 workspace 隔离正确率

### 7.2 接口原则

- Embedder、LLM、Retriever 均可替换。
- 先固定评测集，再接真实模型。
- 不本地训练大模型，优先使用 RAG + Prompt + API/本地小模型。
- 知识库按 workspace 隔离，同一 workspace 更新一次、多平台客服共享。

## 8. 第六阶段：全平台统一客服中台

客服线与运营线并行推进，不等所有平台真实接入才建立领域模型。

### 8.1 统一客服领域模型

```text
conversations
messages
external_customer_refs
conversation_tags
```

平台订单、商品和售后作为关联事实，不把所有内容混在单表中。

### 8.2 回复模式

默认从人工辅助开始：

1. 建议回复：AI 生成，人工发送。
2. 辅助回复：AI 生成，人工编辑后发送。
3. 自动回复：仅在明确授权、敏感词/售后拦截、审计和人工接管机制完备后启用。

### 8.3 客户画像边界

手机号或平台账号不能直接等同于同一客户。需要脱敏/哈希、授权边界、合并规则、冲突处理、数据保留、删除和导出机制。

## 9. 第七阶段：平台连接器与真实客服渠道

建议顺序：

```text
mock/CSV/JSON 回放
→ 淘宝/抖音只读连接器（有授权后）
→ 京东/拼多多只读连接器
→ Amazon/FBA 只读扩展
→ 真实客服渠道 Adapter
```

统一实现：

- 平台账户/商品/SKU 映射
- 快照和事件 Inbox
- 幂等、版本、重试、轮询补偿、对账
- 原始载荷引用和同步运行记录
- 凭证隔离、验签、限流和审计

坚决不实现自动交易写回。

## 10. 第八阶段：产线、出库与跨境扩展

### 10.1 产线排期

先提供理论建议：

```text
理论生产天数 = 补货量 ÷ 产线日产能
最晚启动日期 = 需求日期 - 理论生产天数 - 缓冲天数
```

必须标记：

> 理论排产建议，不代表正式生产计划。

### 10.2 后续业务

- 出库、发货、退货和库存预占
- 运营分析 Agent/Dify 工作流（只输出建议）
- 飞书/钉钉操作卡片
- Amazon/FBA
- 多平台商品上下架建议（人工确认）

## 11. 固定执行顺序（优化版 v2.0）

```text
0. Workspace 隔离与数据库迁移稳定（已完成首轮）
   ↓
1. 统一外部事实层：平台/店铺、SKU、订单、订单明细、每日销量聚合
   ↓
2. 智能运营驾驶舱 MVP：库存健康 + 确定性补货建议
   ↓
3. 补货决策闭环：运营确认/修改 + 运营主管审批 + 采购申请
   ↓
4. 活动销量预测：活动录入 + 人工/历史倍率 + 活动补货缺口
   ↓
5. 库存策略、告警与对账补强
   ↓
6. RAG 固定评测与模型抽象
   ↓
7. 统一客服领域模型与 mock 多平台会话回放
   ↓
8. 真实 LLM/Embedding 适配
   ↓
9. 授权平台只读连接器：淘宝/抖音优先，再扩展京东/拼多多/Amazon
   ↓
10. 真实客服渠道接入
   ↓
11. 产线排期、出库/发货/退货和跨境扩展
```

## 12. 重要约束

- 不拼单平台智能客服，核心竞争力是跨平台统一运营和补货决策。
- 不拼复杂 WMS，仓库只实现轻量协同和可追溯台账。
- 不本地训练大模型，先采用 RAG + Prompt + 可替换模型接口。
- 所有数据按 workspace 隔离；客户端 header、URL 或 Agent Context 不能作为可信租户边界。
- 运营专员是主要日常用户，驾驶舱是登录后的默认首页。
- 预测和效率指标只作为目标，必须以固定评测和真实埋点验证，不把 mock 数字写成生产指标。
- 外部平台只读优先，不自动下单、付款、退款、改价或取消订单。
- 外部平台库存与本系统内部库存分开表达，保留来源、同步时间、事件版本和对账状态。
- Agent 只给建议，库存写入、补货审批、平台交易写入必须人工确认。
- Schema 变更统一使用 Alembic；生产路径不依赖 `create_all` 重建数据库。
- 不直接覆盖库存余额或删除库存流水，库存变更必须可追溯、可重算。
- 任何恢复操作优先保留工作区和数据库，先诊断再修复。

## 13. 验证与恢复

```bash
cd /d D:\ai\dianshang
python -m pytest --collect-only -q
python -m pytest -q
python init_db.py
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
curl http://127.0.0.1:8000/health
curl http://127.0.0.1:8000/ready
```

实施新功能前先检查：

```bash
git status --short
```

服务异常时先保存 traceback，再检查 `.env` 的 `DATABASE_URL`、MySQL、`alembic current` 和端口占用。不要执行 reset、clean、强制覆盖或未经确认的 commit。
